--allow-all-toolsは、ビルトインのツールに効くだけで、MCPサーバーには効かない。
Windows環境だと、エージェントファイルでtoolsを配列にしたり、省略するとエラーになる。
そのため、以下のようにして書く必要がある
```yaml
tools:
    - tool1
    - tool2
```

MCP設定ファイルの記載の仕方
注意点：少なくとも、Windows環境においては、envにて直接値を記入しても認識されないよう。そのため、環境変数経由で渡す必要がありそう　2025/12/09